{
  "name": "ptt-pocket",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "start:lan": "node scripts/serve.mjs --lan",
    "test": "node --test tests/*.test.mjs"
  }
}
